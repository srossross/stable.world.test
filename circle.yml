machine:
  post:
    - curl http://probably.stable.world/install | sudo bash -s -- latest
    - stable.world info

dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
    - python -c "import flask; print(flask.__version__)"
